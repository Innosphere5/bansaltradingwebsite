export const metadata = {
  title: "Shopping Cart",
  description: "Review your wholesale grocery order and proceed to checkout at Bansal Trading.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function CartLayout({ children }) {
  return children;
}
